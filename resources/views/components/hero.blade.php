<section class="hero">
    <p class="hero__subtitle">
        @lang('home.hero.subtitle')
        <img src="{{ asset('/images/wave.webp') }}"
            alt="@lang('home.hero.wave')"
            class="hero__subtitle--wave"
        >
    </p>
    <p class="hero__title">
        @lang('home.hero.title')
    </p>
</section>
